import React from 'react';
import { PlaceholderScreen } from '../components/PlaceholderScreen';

export function SearchScreen() {
  return (
    <PlaceholderScreen
      icon="search"
      title="Search"
      subtitle="Find traders (@username), stocks ($TICKER), and hashtags (#topic)."
    />
  );
}
